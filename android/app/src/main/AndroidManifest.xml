<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No permissions: no network, no external storage. Everything the
         app touches is bundled or in its private directory. Phase 3 adds
         the Storage Access Framework for measured-data import, which
         needs no manifest permission either. -->

    <!-- Both icon attributes, because minSdk is 24: API 26+ picks the
         adaptive icon in mipmap-anydpi-v26, and 24-25 fall back to the
         PNGs. All of them come out of packaging/make_icon.py, from the
         same source.png the desktop icon does. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
