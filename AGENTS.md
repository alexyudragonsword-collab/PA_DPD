# padpd 协作规则

> 本项目使用 Project Cairn 组织项目知识:`AGENTS.md` 是规则与导航入口,
> `cairn/` 是项目知识/状态层。
> 同目录的 `CLAUDE.md` 只含一行 `@AGENTS.md`,使 Claude Code 读到同一套
> 规则;Codex 直接读本文件。

## 项目一句话

WiFi 7(802.11be)PA 行为建模与数字预失真的 AI 辅助研发平台:经典/神经/
样条 PA 模型、ILA/DLA 预失真、前端损伤专用抵消环、定点部署与 PA/DPD 联合设计。

## 初始化配置

- Graduation provider:暂缓对接(首次毕业时再接知识库)
- 知识库索引:尚未配置
- 毕业目标:尚未配置

## 进入项目后的阅读顺序

1. 先读本文件(AGENTS.md)。
2. 读 `cairn/LOG.md` 顶部最近条目(倒序,最新在上)了解近期进展与关键决策。
3. 按任务需要读 `cairn/` 知识专题文档;**动手写代码前必读
   `cairn/工程约束与陷阱.md`**。
4. `cairn/ROADMAP.md` 不存在——本项目的路线图在 `docs/03_roadmap.md`。

## 本项目硬约束(每次动手都适用)

- **开工前确认工作区**:容器可能中途回收,工作区会静默退回旧提交。发现
  文件"消失"或 `git log` 对不上时:
  `git fetch origin <branch> && git reset --hard origin/<branch>`。
- **验证后才能说完成**:
  `QT_QPA_PLATFORM=offscreen python -m pytest tests/ -m "not slow" -q`
  (约 4 分钟);改了神经/训练/ONNX 路径再跑全量。测试失败如实报。
- **不要删**:`tests/conftest.py` 的 `OMP_NUM_THREADS=1` 与 pandas 预导入、
  `tests/test_gui_qt.py` fixture 的显式拆除——都是死锁修复;docstring 里的
  失败记录与设计理由是资产。
- **新增中文 UI 串必须补 `gui_core/i18n.py` 英文**,否则 AST 扫描直接红。
- **线性参数模型用 LS 闭式解,不要用 SGD**(同一 GMP 用 SGD,ACLR 差 9 dB 以上)。
- **Qt 侧耗时操作走 `FnWorker`**,且在 GUI 线程上先把控件值快照出来再传进
  闭包;算法放 `gui_core/services.py`(纯 Python,不 import GUI)。
- **性能数字改动三处同步**:`README.md`、`docs/05_performance_summary.md`、
  `manual/zh|en`。
- 代码与注释英文,注释写"为什么";数值断言写实测值并注明来源
  (`assert gain > 6.0   # measured +10.5`)。`docs/`、`manual/zh` 中文,
  `manual/en` 同批改。
- 提交信息写清**为什么**与实测数字;修 bug 写根因,不写 "fix test"。
- 未获明确许可不要推 main、不要打 tag、不要开 PR。

> 完整陷阱清单(numpy atol、首采冻结校准的电平次序、CI 依赖集差异、状态
> 模型评估约定、相位谐波不可互表)见 `cairn/工程约束与陷阱.md`。

## 文档职责

| 文件 | 角色 | 维护方式 |
|---|---|---|
| `AGENTS.md`(根) | 规则与导航 | 很少变动,≤ 60 行 |
| `CLAUDE.md`(根) | 一行 `@AGENTS.md` | 写一次,不再改动 |
| `cairn/LOG.md` | 时序日志 | 新条目加在顶部,每条 ≤ 20 行,只写摘要+指针 |
| `cairn/<topic>.md` | 知识专题文档(当前真相) | 就地更新;坑放在正文小节并用 `contains` 标注 |
| `cairn/Reference/` | 外部原始输入 | 按需创建,只追加 |
| `cairn/Cited.md` | 知识库引用清单 | 只放指针,不复制原文 |

> 其余内容只在出现具体信号时创建,不预建空壳。工程资产(被代码或流程消费
> 的契约/配置/规格)不归本系统管,留在代码树里。
>
> 与既有文档的分工:`CONTRIBUTING.md` 面向人类贡献者(装环境、提 PR、
> 代码风格);`docs/` 是工程文档与路线图;`manual/` 是面向用户的双语手册;
> `cairn/` 只装**过程知识**(决策理由、踩坑经过、当时为什么这么选)。

## 冲突仲裁规则

- 优先级:**知识专题文档 > LOG 历史**;规则级冲突由本文件裁定。
- 业务/设计结论以 `cairn/` 专题文档的最新记录为准,不以更早的 LOG 条目为准。

## 知识库消费反射

- 在开展"其可复用内核够格毕业"的工作前,先查阅本项目自己的 `cairn/` 知识
  专题文档;当前尚未对接外部知识库(provider 暂缓对接——见上方初始化配置),
  外部索引检查与 `cairn/Cited.md` 引用在接上知识库后启用。

## 文档协作规则

- 动手改之前先判断用户要的是"讨论/建议"还是"直接改文档";用户说"先看看/
  先评估"时,先给分析,不要直接重写正式文档。
- 纠正过去的判断时追加更正说明,不要静默覆盖。
- 不要把未经确认的判断写成既定事实。

## 知识沉淀规则

- 每次有实质进展后,在 `cairn/LOG.md` 顶部加一条(摘要+指针);结论沉淀进
  `cairn/` 知识专题文档。
- **完成回复门槛**:在做出任何完成类声明前(包括但不限于工作完成/已实现、
  已定稿、已更新、已同步、已验证或测试通过、问题已修复/已解决、交付物可用、
  工作已结束,以及语义等价的说法),先执行 `references/maintenance.md` 的
  Cairn checkpoint;只更新其触发矩阵要求的记录,核验后再回复。明确的只读/
  不改动要求禁止 Cairn 写入。
- 跨项目可复用经验通过毕业机制沉淀——本项目暂缓对接知识库(见上方初始化配置)。
